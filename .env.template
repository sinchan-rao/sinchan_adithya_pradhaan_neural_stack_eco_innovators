# Google Maps API Key (Optional)
# If you have a Google Maps Static API key, paste it below for faster imagery fetching
# Leave blank to use browser automation mode (slower but works without API key)
GOOGLE_MAPS_API_KEY=
